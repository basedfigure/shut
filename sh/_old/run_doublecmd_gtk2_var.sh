#!/bin/bash

# I first got into Linux around GNOME 2, circa ~2005, and the themings just kept
# getting worse and worse as modern UI:s took over most distros, so it's good th
# at MATE and TDE exist--with mate dropping the ball years ago on keeping it old
# school, and intact. There are many other old-school distros with sleek and old
# GUI:s, but GNU Linux is too vast for me to get into them, in a way that I care
# to do.


# I used to hack two GTK themes together in WMLive, by setting up widgets from a
# user edited Clearlooks, here:  https://github.com/osch/clearlooks-classic
# ...
#
# ..and running my quickly modded "greylooks" one, by riffing off of (url): 
# github.com/raorn/OneStepBack
#
# ..to launch the two together in Double Commander (e.g):
GTK2_RC_FILES=~/.themes/greylooks/gtk-2.0/gtkrc doublecmd